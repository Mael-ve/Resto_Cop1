services: 
  site: 
    build: .
    ports:
      - ${PORT}:${PORT}
    volumes: []
    environment:
      MARIADB_HOST: mariadb
      MARIADB_USER: root
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_DB: ${DB_NAME}
    depends_on:
      - mariadb

  mariadb:
    image: mariadb:10.11
    volumes:
      - db_site:/var/lib/mysql
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD}
      MARIADB_DATABASE: ${DB_NAME}

volumes:
  db_site: